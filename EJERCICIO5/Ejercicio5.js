funcionImprimir(500)//llamado a la funcion con el envio de un parametro 
funcionDividirnumero(183)

function funcionImprimir(numero) // variable
{
    console.log(numero) //se imprime nro
}

function funcionDividirnumero(numero)
{
    console.log(numero/2)
}